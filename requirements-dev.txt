-r requirements.txt
pytest>=8.0
pillow>=10.0   # deploy/gen_icons.py only (favicon.ico bundling)
# playwright is a runtime dependency (see requirements.txt); the browser
# suite additionally needs the browser:  playwright install chromium
